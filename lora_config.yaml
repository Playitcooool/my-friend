model: "/Volumes/Samsung/lmstudio/lmstudio-community/lmstudio-community:Qwen3.5-4B-MLX-8bit"
train: true
fine_tune_type: lora
data: "./data"

batch_size: 1
iters: 1000
learning_rate: 1e-5
num_layers: 4
max_seq_length: 1024
grad_checkpoint: true
mask_prompt: true

steps_per_report: 10
steps_per_eval: 100
val_batches: 25

adapter_path: "./adapters/huxinyi"
save_every: 100

lora_parameters:
  keys: ["self_attn.q_proj", "self_attn.v_proj"]
  rank: 8
  scale: 20.0
  dropout: 0.0
